App As appinfo:
    BackEnabled: =true
    Formulas: |-
        =// Set theme colors
        PrimaryColor = If(varAppColorMode = ""
    OnStart: |-
        =Set(
            varGridEdit,
            false
        );
        Set(varNewRecord,Defaults(Annual_Schedule_QA));
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

