App As appinfo:
    BackEnabled: =true
    Formulas: |-
        =// Set theme colors
        PrimaryColor = If(varAppColorMode = ""
    OnStart: =
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

